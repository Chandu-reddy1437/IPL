services:
  - type: web
    name: ipl-predictor
    runtime: node
    buildCommand: npm install -g pnpm && pnpm install
    startCommand: node server-simple.js
    envVars:
      - key: NODE_ENV
        value: production
